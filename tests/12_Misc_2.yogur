// A little bit of everything

var a: Int = 3
var b: Int = 5

var c: Int = 0

while (a == b) && true {
	for i in 1 to 10 {
		c = c + 1
	}
}

var d: Int[10]

def patata(arg1: Int, arg2: Bool) {
	patata(arg1 + 1, arg2 || true)
}

class Class {
	var e: Int[10]
	
	def classPatata(arg1: Int) -> arg2: Bool {
		c = c + 1
		b = b / b
		e[b] = e[3]
		arg2 = c == b
	}
}